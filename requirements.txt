# Core Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.6.0
pydantic-settings==2.1.0

# Template Engine
jinja2==3.1.3

# HTTP Client
requests==2.31.0

# AI/LLM APIs (OpenAI provider is optional; Ollama uses plain HTTP)
openai==1.10.0

# Utilities
python-multipart==0.0.9
python-dotenv==1.0.1

# Testing (dev)
pytest==8.0.0

# NOTE: The decision tree is now rendered interactively in the browser
# (vis-network), so matplotlib / networkx / pygraphviz are no longer required.
